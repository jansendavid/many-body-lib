#pragma once
#include "timeev.hpp"
namespace TimeEv{
  // template <typename State, typename Matrix >
  // State timeev_exact(State& initialState, const Matrix& hamiltonian, const Matrix&  observable, double dt, size_t numberOfSteps);
}
