#pragma once
#include"files.hpp"
#include"Eigen/Dense"
#include<vector>
template<typename T>
void offd_vs_d(T const filename, size_t steps)
{
  Eigen::MatrixXcd M;
  bin_read(filename, M);
  std::vector<double>
    std::vector<size_t>
		
}
